# [Критерий 1][Метрика М1][Уровень L1]
# Зачем: локальный trust-store для verify подписи артефакта в WARN-режиме перед Promote/Deploy.

# Фиксированный OIDC-issuer GitHub Actions.
# Зачем: принимаем только сертификаты, выпущенные через GitHub OIDC.
OIDC_ISSUER=https://token.actions.githubusercontent.com

# Пин сертификатной идентичности для cosign verify.
# Зачем: доверяем РОВНО подписям, сделанным этим workflow-файлом на ветке main в этом репозитории.
# owner/org  : MainMuck
# repo       : Ci.cdcompany
# workflow   : .github/workflows/build-sign-verify.yml
# branch/ref : refs/heads/main
CERT_IDENTITY=https://github.com/MainMuck/Ci.cdcompany/.github/workflows/build-sign-verify.yml@refs/heads/main
